
"""
projekt_1.py: první projekt do Engeto Online Python Akademie

author: Jana Veselá
email: janca.vesela90@gmail.com
"""

#přiřaď user k password
users = {
    "bob":"123",
    "ann":"pass123",
    "mike":"password123",
    "liz":"pass123"
}

#vloz username, pokud souhlasi, vloz heslo, kdyz souhlasi pokracuj dal, kdyz nesouhlasi ukonci program
jmeno = input("username") 
heslo = input("password")
if users.get (jmeno) == heslo:
    print("Welcome to the app",
    "We have 3 texts to be analyzed")
else:
    print("unregistered user, terminating the program.")
    exit()

# separator
print(70 * "-")

#text obsahujici 3. texty
text_1 = '''Situated about 10 miles west of Kemmerer 
Fossil Butte is a ruggedly impressive
topographic feature that rises sharply
some 1000 feet above Twin Creek Valley
to an elevation of more than 7500 feet
above sea level. The butte is located just
north of US 30N and the Union Pacific Railroad,
which traverse the valley.'''

text_2 = '''At the base of Fossil Butte are the bright
red, purple, yellow and gray beds of the Wasatch
Formation. Eroded portions of these horizontal
beds slope gradually upward from the valley floor
and steepen abruptly. Overlying them and extending
to the top of the butte are the much steeper
buff-to-white beds of the Green River Formation,
which are about 300 feet thick.'''

text_3 = '''The monument contains 8198 acres and protects
a portion of the largest deposit of freshwater fish
fossils in the world. The richest fossil fish deposits
are found in multiple limestone layers, which lie some
100 feet below the top of the butte. The fossils
represent several varieties of perch, as well as
other freshwater genera and herring similar to those
in modern oceans. Other fish such as paddlefish,
garpike and stingray are also present.'''
texts = (text_1, text_2, text_3)

#vyber cislo textu, kdyz cislo je spravne pokracuj v analyze, pokud neni nebo je pismeno ukonci program
text = input("Enter a number btw. 1 and 3 to select:")
if text.isnumeric() and int(text) in range(1, 4):
    print("We go analyzed")
else:
    print("is not in our text, close")
    exit()
print(texts[int(text)-1])
selected_text = texts[int(text)-1]

# separator
print(70 * "-")

#odstraneni interpunkce
import string
for punctuation in string.punctuation:
    selected_text = selected_text.replace(punctuation, "")

#spocitej slova v analyzovanem textu
words = selected_text.split()
soucet = len(words)
print("There are", {soucet}, "words in the selected text.")

#počet slov začínajících velkým písmenem
#počet slov psaných velkými písmeny
#počet slov psaných malými písmeny
#počet čísel (ne cifer),
#sumu všech čísel (ne cifer) v textu.
islover_count = 0
isupper_count = 0
start_isupper = 0
numbers_count = 0
numbers_sum = 0
slova = {}
for word in words:
    if word.isalpha():
        if word.islower():
            islover_count += 1
        if word.isupper():
            isupper_count += 1
        if word[0].isupper():
            start_isupper += 1
    if word.isnumeric():
        numbers_count += 1
        numbers_sum += int(word)
    word_len = len(word)
    if word_len in slova:
        slova[word_len] += 1
    else:
        slova[word_len] = 1


#pocet slov zacinajici velkym pismenem
print("There are", {start_isupper},"titlecase words.")

#pocet slov psanych velkymi pismeny
print("There are",{isupper_count} ,"uppercase words.")

#pocet slov psaly malymi pismeny
print("There are", {islover_count}, "lowercase words")

#pocet cisel ne cifer
print("There are", {numbers_count}, "numeric strings.")

#sumu vsech cisel (ne cifer) v textu
print("The sum of all the numbers", {numbers_sum})

# separator
print(70 * "-")

print(f"{'LEN':<3}| {'OCCURENCES':^18}| {'NR.':<3}")
# separator
print(70 * "-")

#četnost různých délek slov v textu

for word_len in sorted(slova):
     print(f"{word_len:<3}| {(slova[word_len]* '*'):<18}| {slova[word_len]:<3}")
